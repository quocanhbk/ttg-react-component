import styled from 'styled-components'

const OneOption = styled.option`

`;
const Option = (props)=>{
    return(
        <OneOption>
            One
        </OneOption>
    )
}
export default Option